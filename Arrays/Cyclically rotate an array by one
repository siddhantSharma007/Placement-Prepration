class Compute {
    
    public void rotate(int arr[], int n)
    {
        for(int i =1;i<n;i++)
      {
          int swap = arr[i];
          arr[i] = arr[0];
          arr[0] = swap;
      }

    }
}
